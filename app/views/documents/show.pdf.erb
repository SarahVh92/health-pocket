<!DOCTYPE html>
<html>
  <head>
    <title>HealthPocket</title>
    <meta charset="utf-8" />
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type"/>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= wicked_pdf_stylesheet_link_tag "show" %>

  </head>
  <body>
    <div id="header">
    </div>
    <% @sentences.split(",", -1).each do |sentence| %>
      <%= sentence[1..-2].gsub(/((&quot;)|"|(&quot))/, "") %>
    <% end %>
  </body>
</html>
