<%= simple_form_for @document do |f| %>
  <%= f.input :doctor_name %>
  <%= f.input :doc_type, collection: Document::DOC_TYPES, label: 'Update Document' %>
  <%= f.submit "Update Document", class: "btn btn-primary" %>
<% end %>

<%= @document.photo %>
