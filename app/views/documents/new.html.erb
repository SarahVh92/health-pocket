<%# <% content_for :meta_title, "#{@document.name} is on #{DEFAULT_META["meta_product_name"]}" %>
<%# <% content_for :meta_description, @document.description %>
<%# <% content_for :meta_image, cl_image_path(@document.photo.key) if @document.photo.attached? %>

<div class = "container-sm p-4 mt-3">
<%= simple_form_for @document do |f| %>
  <%= f.input :doctor_name %>
  <%= f.input :country %>
  <%= f.input :date, html5: true %>
  <%= f.input :doc_type, collection: Document::DOC_TYPES, label: 'Add category' %>
  <%= f.input :comment %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit, "Upload Document", class: "button" %>
<% end %>
